# Handoff, morning of 2026-09-13

Read [CLAUDE.md](CLAUDE.md) first. This note covers one session, the evening of 2026-09-12,
and it supersedes nothing in [HANDOFF_2026-09-12.md](HANDOFF_2026-09-12.md) except where it
says so. Run `python scripts/corpus_status.py` for live numbers rather than trusting any
count below.

## 1. What needs your eyes, in order

1. **Eleven `tail` boundaries**, listed in
   [data/rule7_contested_boundaries.md](data/rule7_contested_boundaries.md) with a `?t=`
   link eight seconds early on each. These are the side of the defect you found in ep63 at
   1:58:44: words left at the END of one speaker's block that belong to the next speaker.
   Some are real interruptions and should stay as they are -- ep62's `White pap-` /
   `dalam white paper pun sama juga` is one.
2. **Four boundaries decided on tool evidence while you slept**, recorded in
   `data/speaker_adjudications.json` under `rule7_tool_decided_2026_09_12`: ep33 22:47
   (`salahlah.` to Wong Chen), ep46 39:29 (`komen satu.` to Wan Afiq), ep47 2:54:59
   (`kal- saya kita tak nak` to Rafizi), ep49 00:46 (`Speaker ?` named as Haziq). Each had
   the same three signals and each matches a shape you ruled on yourself the same evening.
   If any is wrong, say which and it comes straight back out.
3. **ep61's published passage, 01:07-02:25**, rewritten BY HAND in all three interview
   files to match the rebuilt raw. `rewrite_segments.py --write` refuses unless every
   segment of the episode has an accepted rewrite, so a 90-second fix would have meant a
   whole-episode pass. It reads as newspaper copy, but it is my prose, not the pipeline's.

## 2. The ep61 finding, which is the important one

raw.md's 01:30-03:00 was never your verified text. Your 18 dictated turns have been in
`data/speaker_ground_truth.json` since 2026-08-30; `mai_camera_raw.py`'s `carve_in_gold()`
splices the CURRENT raw.md's blocks for the region's time range, and for ep61 that meant
the old local-ASR blocks. Three of your turns were fused into one, `Mana ada cuti?` was
missing from every file, and so was the `YB?` in `Macam nak pilihan raya dah YB?`.

`scripts/apply_gold_turns.py` now writes a dictation over MAI's words for the passage. Four
things it had to get right, each of which failed first:

- **One global monotonic alignment**, not a per-turn search. `lib_locate` found 6 of 18,
  because it returns None below three tokens, so `Ya.` and `Kita memang beria.` can never
  be located that way.
- **The ratio is scored against the dictation**, not the padded window. The region's
  minutes are the OLD clock: turn 1 sits at 66 s against a stated 01:30.
- **Which raw blocks get replaced is decided by word seams, never by the clock.** By clock
  it selected 24 blocks for an 18-block passage, and the only guard checked the words
  OUTSIDE that range -- four minutes of unrelated talk would have been deleted silently.
- **A trailing word that opens the next turn is handed forward, fuzzily**, because you write
  `Okay` where MAI writes `Okey`.

Turns 10 and 11 were revised to your 2026-09-12 reading (the second `macam nak pilihan
raya` is Rafizi's). The older text is kept in the entry's own note.
`data/speaker_from_gold.json`'s 01:55 was corrected from Rafizi to Haziq: it was written
from a stamp under the drifting clock. `check_owner_decisions.py` reports ep61 with 0
mismatched, down from 2.

**Checked, and ep61 is the only one.** `data/speaker_ground_truth.json` holds exactly one
entry with dictated turns, ep61's. ep23 has only a REQUEST document
(`data/gold_passage_request_ep23.md`, the passage at 08:30-10:00 you were asked to dictate
and never did), and ep62's owner-verified passage is recorded differently, not as turns. So
no other episode carries this defect, and the second gold passage for ep23 is still open
work if you want a second measured sample.

## 3. Rule 2 now has a checker

`check_agencies.py` + `data/agency_roster.json` (36 bodies, a source URL each) runs inside
`qa_check.py`. Four defects fixed through `fix_proper_nouns.py`: 16 `Kementerian Keuangan`
(the Indonesian spelling), ep09's published `keuangan`, ep15 `Menteri Kawangan`, ep27
`Jabatan Perkuam Negara`, and ep05's `Akta SPRM 2009` in a passage about appointing the
Chief Justice (the law is the Judicial Appointments Commission Act 2009; raw.md never says
SPRM). The roster itself was wrong once -- it carried `Keselamatan Makanan` from Wikipedia
where kpkm.gov.my says `Keterjaminan` -- so an agency's own site outranks any third party.

## 4. Rule 7's detector

`check_overlap_boundaries.py`, report-only. Corpus-wide: 0 contested, 44 attested by the
camera, 8 settled by your rulings, 11 on the `tail` side. Four corrections it needed, all
from your reading:

- The unit is a PAIR of blocks, not the A/B/A sandwich the rule's prose describes (the
  sandwich occurs once across the adopted episodes; the pair 66 times).
- The verdict is taken at the boundary, not over the turn.
- The block's own first second is skipped, because the cut lags speech by about two seconds.
- One stray second of the previous speaker is the cut, not a claim on the words.

## 5. YouTube captions: tested and rejected, with numbers

The `json3` caption format carries `isSpeakerChange` flags; the `.vtt` this repo downloads
drops them, which is why nobody had seen them. Measured against your own 11 rulings it
agrees on 4, and the best offset/tolerance sweep reaches 7 with the offset fitted to those
same cases. Per-episode chance rate is 13% to 32%, because the flags fire every few
seconds. Full numbers in ARCHITECTURE.md. Do not re-test on the same data.

## 6. Corpus state and running jobs

- 70 episodes, MAI words for all 70, 23 camera references, 22 raws adopted.
- The GPU chain `data/_chain_ep43_ep39.out` is running ep42 (about 5 chunks left at 23:10),
  then ep40, then ep39. Expect it to finish in the early morning.
- **A queued job will adopt ep43, ep42, ep40 and ep39 as soon as that chain prints
  `finished:`** -- `data/_queue_adopt_after_chain.sh`, output in `data/_adopt_queue.out`. It
  waits by grepping the chain log every 300 s, never by checking a process. Look at that
  output before starting anything on the GPU.
- Cleanup freed 6.41 GB: decoded WAVs and frame clips for the 22 adopted episodes, plus the
  pre-adoption candidate files. The WAVs for ep42, ep40, ep39, ep43 were deliberately kept.

## 7. Standing decision recorded this session

Bringing an unadopted episode up to the ep62 standard happens by REPROCESSING it with the
camera reference, never by hand-patching its local-ASR raw, because adoption rebuilds raw
and regenerates the interview files. Owner's words: "no rush. take it slow one by one."
Measured gap on the 11 local-ASR episodes touched this session: 8 filler-only turns, 1
adjacent same-speaker pair, and 27 to 99 mid-sentence cross-speaker cuts each, against 3
per adopted episode.
